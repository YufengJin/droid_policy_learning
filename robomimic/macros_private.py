"""
本地覆盖 robomimic/macros.py 中的变量（勿提交含真实 API key 的版本到公共仓库）。

推荐：WANDB_API_KEY = None，在终端执行 `wandb login`，凭证写入 ~/.netrc。
若必须在文件里写 key，可设置 WANDB_API_KEY = "..."（仅本机保留）。
"""
DEBUG = False
VISUALIZE_RANDOMIZER = False
WANDB_ENTITY = "yufeng-jin"
WANDB_API_KEY = None
